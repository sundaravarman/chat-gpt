const fetchEnableTableData = async () => {
    setLoading2(true);
    try {
        // Replace with your backend API endpoint for fetching table data
        const apiUrl = `http://127.0.0.1:5000/Extention_table_data?selectedOption1=${selectedOption1}&selectedOption2=${selectedOption2}&selectedOption3=${selectedOption3}`;

        const response = await axios.get(apiUrl);

        // Check if the response data is valid JSON and not empty
        if (response.data && Array.isArray(response.data) && response.data.length > 0) {
            setTableData2(response.data);
        } else {
            // Handle the case where response data is empty or not an array
            setTableData2([]);
            console.warn('No data returned from the backend or data is not in expected format.');
        }

        console.log(response.data);
    } catch (error) {
        console.error('Error fetching table data:', error);
        // Optionally set table data to an empty array on error
        setTableData2([]);
    }
    setLoading2(false);
};
